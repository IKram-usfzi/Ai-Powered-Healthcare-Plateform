# Diagrams

Mermaid/PlantUML sources for the 17 mandatory diagrams (`docs/architecture.md` §6),
plus exported images where a diagram needs to be embedded as a static asset.

Draw.io is reserved for presentation-specific layout needs (ADR-008); everything else
here should be diagrams-as-code so changes are diffable in Git.

Populated in Phase 9 (`docs/impmemnentaion-plan.md`).
