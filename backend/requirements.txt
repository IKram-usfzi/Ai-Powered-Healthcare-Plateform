fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

redis==5.2.0
prometheus-fastapi-instrumentator==7.0.0

python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since 2020) reads bcrypt.__about__.__version__ for
# backend detection; bcrypt>=4.1 removed that attribute, breaking every hash/verify
# call. Pin to the last compatible 4.0.x release until passlib is replaced/updated.
bcrypt==4.0.1
python-multipart==0.0.32

# --- Module 4: AI-Assisted Health Risk Assessment (Phase 5, api-spec.md §6) ---
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2

# --- dev / lint / test (see docs/developement-rules.md §3, §7 and docs/Testing-startegy.md) ---
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
black==24.10.0
ruff==0.8.4
